* NOTE *

This is the recommended location (/bin) to execute all the binary files.

If you are in Windows: copy files in "/bin/i386-win32" directory into this location.

If you are in Linux: copy files in "/bin/i386-linux" directory into this location.
